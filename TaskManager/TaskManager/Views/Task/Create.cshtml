@{
    ViewData["Title"] = "Index";
}
@using TaskManager.BLL.DTO

<meta charset="utf-8" />
<div class="content">
    <div class="task">
        <h2>Заполнение карточки</h2>
        <form method="post">
            <p>
                <label>Название:</label><br />
                <input type="text" name="Title" />
            </p>
            <p>
                <label>Описание:</label><br />
                <input type="text" name="Description" />
            </p>
            <label>Приоритет:</label><br />
            @Html.DropDownList("priority", Html.GetEnumSelectList(typeof(PriorityDTO)))

            <label>Назначена на:</label><br />
            
            @*<select name="employee" class="selectpicker">
                @foreach (EmployeeDTO b in Model)
                {
                    <option>@b.Name</option>
                }
            </select>*@
            @*<select 
                    class="form-control"
                    asp-items="@(new SelectList(@ViewBag.temp,"Name"))"></select>*@
                      
            <select name="empl" asp-items="ViewBag.Temp"></select>

            <input type="submit" value="Отправить" class="btn" />

        </form>
    </div>
</div>